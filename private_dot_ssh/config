# Machine-specific configs (not synced)
Include config.local
Include config.d/*

# Global defaults
Host *
    ForwardAgent yes
    ControlMaster auto
    ControlPath /tmp/%r@%h:%p
    ControlPersist 2h
