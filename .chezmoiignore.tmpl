LICENSE
README.md
CLAUDE.md
install.sh
legacy/
.claude/

# Ignore scripts directory (run_* scripts are in repo root)
scripts/

# Plaintext versions of encrypted files are for local editing only, never deployed
**/decrypted_*

{{ if not (and (hasKey . "decrypt_active") .decrypt_active) }}
# Skip encrypted files when decryption is not active
# (either profile doesn't want it, or age key not yet available)
# NOTE: patterns match TARGET paths — chezmoi strips encrypted_/private_/dot_/.tmpl/.age
.ssh/config.d/00-trusted
.config/git/conf.d/config
.config/git/conf.d/oss
.config/git/conf.d/work
.config/fish/conf.d/zz_00-work.fish
{{ end }}
