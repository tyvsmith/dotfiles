LICENSE
README.md
CLAUDE.md
install.sh
legacy/
.claude/

# Ignore scripts directory (run_* scripts are in repo root)
scripts/

# Plaintext versions of encrypted files are for local editing only, never deployed
**/decrypted_*

{{ if not (and (hasKey . "decrypt_active") .decrypt_active) }}
# Skip encrypted files when decryption is not active.
# When encryption is not configured, chezmoi retains the .age suffix in
# target paths, so **/*.age matches them. When encryption IS configured,
# chezmoi strips .age and these patterns harmlessly match nothing.
**/*.age
{{ end }}
