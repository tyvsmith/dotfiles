LICENSE
README.md
CLAUDE.md
Brewfile
install.sh
legacy/
.claude/

# Ignore scripts directory (run_* scripts are in repo root)
scripts/

# Plaintext versions of encrypted files are for local editing only, never deployed
**/decrypted_*

{{ if not .should_decrypt }}
# Skip encrypted files when decryption is disabled
**/*.age
**/encrypted_*
{{ end }}
