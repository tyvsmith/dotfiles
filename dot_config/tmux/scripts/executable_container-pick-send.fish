#!/usr/bin/env fish
# container-pick-send.fish — tmux popup wrapper for container-fzf
# Runs the multi-action container picker inside a tmux popup.
# The popup provides the TTY that fzf needs.

container-fzf --tmux
