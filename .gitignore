*.local
*.local.json
!create_*.local
vim/bundle/*
.env
# Most editor/OS/AI-agent noise is handled by ~/.config/git/ignore (global).
# Keep only repo-specific overrides here.
.claude/
.omc/
# Decrypted plaintext copies for editing; encrypted .age files are committed
**/decrypted_*
.mcp.json
.mcp.json.bak
