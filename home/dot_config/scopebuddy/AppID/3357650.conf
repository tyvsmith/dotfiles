# PRAGMATA (appid 3357650)
#
# Common settings and the wrapper chain come from ../scb.conf.

# Game argument, appended after %command%. Capcom RE Engine: disables the
# game's Wine detection, which is what unlocks DLSS and frame generation.
command="$command /WineDetectionEnabled:False"
