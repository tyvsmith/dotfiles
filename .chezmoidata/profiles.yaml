# =============================================================================
# Machine profiles — single source of truth for machine configuration
# =============================================================================
#
# Each profile fully defines a machine setup:
#   desc:     human-readable description (shown during profile selection)
#   tags:     comma-delimited category tags (core, container, dev, ai, experiment, hardware, ui, ui-extra, gaming)
#   brew/pacman/apt/dnf/rpm_ostree/flatpak/appimage: which package managers to enable
#   work:     work machine (system SSH agent, corporate configs)
#   decrypt:  enable age decryption of private configs
#
# Profile is selected interactively during `chezmoi init`, or via:
#   DOTFILES_PROFILE=arch chezmoi init
#   chezmoi init --promptChoice profile=arch
#
# Auto-detected from distro if non-interactive.
#
# NOTE: Adding a new profile here also requires updating:
#   - .chezmoi.toml.tmpl (distro auto-detection, if the new profile needs it)
#   - install.sh is NOT involved — profile selection is handled by chezmoi
#
# Properties (tags, work, decrypt, de, desc) are read directly from this file
# by .chezmoi.toml.tmpl — no duplication needed.

profiles:
  # --- macOS ---
  macos-work:
    desc: "Work Mac — full dev setup with corporate config"
    tags: core, container, dev, experiment, ai, experiment, ui, ui-extra
    brew: true
    work: true
    decrypt: true

  # --- Arch Linux ---
  arch-desktop:
    desc: "Arch Linux desktop — full setup with paru + flatpak"
    tags: core, container, dev, experiment, ai, experiment, hardware, ui, ui-extra, gaming
    de: hyprland-omarchy
    pacman: true
    flatpak: true
    appimage: true
    decrypt: true

  # --- Debian/Ubuntu ---
  debian-server:
    desc: "Debian/Ubuntu server — CLI tools only"
    tags: core
    apt: true

  devpod:
    desc: "Debian/Ubuntu dev — CLI + dev tools via apt"
    tags: core, dev, ai
    work: true
    brew: true
    decrypt: false

  # --- Immutable Fedora ---
  silverblue:
    desc: "Fedora Silverblue/Bazzite — immutable desktop"
    tags: core, container, dev, experiment, ai, experiment, hardware, ui, ui-extra, gaming
    de: gnome
    brew: true
    rpm_ostree: true
    flatpak: true
    appimage: true
    decrypt: true
